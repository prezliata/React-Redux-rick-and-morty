const styles = (theme) => ({
    wrapper: {
        paddingTop: 10,
        margin: '0px auto',
        display: 'flex',
        flexWrap: 'wrap',
        flexDirection: 'row',
        justifyContent: 'space-around',
    }
})

export default styles